import React from "react";
import "./RelatedVideo.scss";
const RelatedVideoList = ({ children }) => {
  return <div className="related_videos_section">{children}</div>;
};

export default RelatedVideoList;
